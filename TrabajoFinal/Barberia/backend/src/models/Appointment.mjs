class Appointment {
    constructor(id, clientId, barberId, date, status) {
        this.id = id;
        this.clientId = clientId;
        this.barberId = barberId;
        this.date = date;
        this.status = status;
    }
}

export default Appointment;